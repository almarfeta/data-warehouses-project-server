version: '3.9'
services:
  oracle-db:
    image: container-registry.oracle.com/database/enterprise:latest
    container_name: oracle-enterprise-db
    ports:
      - "1521:1521"
      - "5500:5500"
    environment:
      - ORACLE_PWD=Parola1234
    restart: no
